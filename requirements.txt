requests>=2.32
PyYAML>=6.0
python-dotenv>=1.0
# Fetcher sigiloso (TLS y cabeceras de Chrome real) para las paginas que
# bloquean scripts. Opcional: si falla la instalacion, todo sigue con requests.
scrapling[fetchers]>=0.4
